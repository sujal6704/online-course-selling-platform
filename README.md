# Online Course Selling Platform

An online course marketplace where instructors can list courses and learners can browse and purchase them.  
This project is built with a **React + Vite + Tailwind CSS frontend** and a **Node.js + Express + MySQL backend**.

---

## 🚀 Features

- 📚 **Course Catalog**
  - View a list of available courses
  - See key info like title, description, price, etc.

- 🧭 **Single Page Application (SPA)**
  - Built with React and `react-router-dom`
  - Smooth client-side navigation between pages

- 🔗 **API Integration**
  - Frontend communicates with backend via REST APIs using `axios`
  - Centralized API base URL for easy config

- 🗄️ **Backend REST API**
  - Node.js + Express server
  - MySQL used for persistent storage
  - Uses `cors` and `body-parser` for handling requests

- 🎨 **Modern UI**
  - Styled with Tailwind CSS
  - Responsive layout (desktop & mobile friendly)

> Note: You can update this section with more specific features as you add them (e.g. authentication, payment, admin dashboard, etc.).

---

## 🧱 Tech Stack

**Frontend**
- React 18
- Vite
- React Router DOM
- Axios
- Tailwind CSS

**Backend**
- Node.js
- Express
- MySQL (via `mysql2`)
- body-parser
- cors

---

## 📁 Project Structure

```bash
online-course-selling-platform/
├── Backend/      # Node.js + Express + MySQL backend
└── Frontend/
